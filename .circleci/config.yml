# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  run-ubuntu:
    docker:
      - image: ubuntu:18.04
    working_directory: /home/stephen2m/repo

    steps:
      - checkout
      - run:
          name: List Directory Contents
          command: |
            pwd; ls -lsha
      - run:
          name: Provision OS
          command: |
            bash setup.sh

      - store_artifacts:
          path: logs

  run-arch-linux:
    docker:
      - image: oddlid/arch-desktop
    working_directory: /home/stephen2m/repo

    steps:
      - checkout
      - run:
          name: list directory
          command: |
            pwd; ls -lsha
      - run:
          name: Provision OS
          command: |
            bash tasks/arch/01_osprep.sh

      - store_artifacts:
          path: logs

workflows:
  version: 2
  provision-environment:
    jobs:
      - run-ubuntu
